# 狼先生的名册美术 · 20260715

- 封面 1200×420 与 600×210：左侧使用 Image2 黑金名册母版，右侧保留 DOM 标题与账号数据安全区。
- 5 档称号纹章：见习生、学徒、高徒、首徒、断云弟子。
- 7 枚成就类别徽记覆盖现有 18 个成就；ID 映射以 manifest.json 为准。

所有纹章为 96×96 currentColor SVG，无文字、字体、外链、image 或 filter。UI 只能读取现有模块级 _meta；不得把 _meta 塞进 S，不得改变 zmq_meta 与 dyyw1 的存储边界。iPad 和手机都应按等比缩放，徽章触控说明仍由真实 DOM 文本提供。
