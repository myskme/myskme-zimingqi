# 自鸣棋 · 视觉自装通道 + 断云集市委托单(Claude → Codex)20260717

> 王老师授权升级协作方式:**你从"交素材"升级为"部分自装"**——下面 A 部分的活,做完可以直接改 `index.html` 上线,不必等我接线。B 部分(断云集市)仍走"你出资产、我接线"。
> 动手前 `git pull --rebase`;日志只追加;每次 push 后确认 CI `自鸣棋自检` 绿(现基线 `#selftest 275/275`)。

---

## A. 自装通道(可直接改 index.html 的表现层专属车道)

这些全是**我留下的程序化占位样式**,你在协作日志里也多次提过想做正式版。现在正式移交:钩子类名和 DOM 结构都已稳定,你可以直接改 CSS(以及下列明确圈定的表现层 DOM),改完自测自推。

按价值排序:

1. **玩家名片正式视觉**——`.pcard`/`.pc-*` 全套(边框/底纹/称号徽章位/镇店之宝陈列位)。现在是素 panel。你 0714 就说想做。
2. **名匠榜正式视觉**——榜行 `.lbrow`/`.hall-art`/`.hall-lab`,前三名金银铜排名徽章;「绝品实录」`.feedrow` 走马灯/横幅样式。
3. **传世开箱大幕正式美术**——`.affix-veil`/`.av-*` 现在是纯 CSS 金光。这是全游戏最高潮的仪式,值得正式母版(可加背景资产,走版本化目录+CSS 引用)。
4. **点名星盘正式化**——`.wp`(名牌)/`.wp.lit`(巡光过)/`.wp.won`(落定)/`#wheel-stage` 中心底纹(你自己提的)。名牌可做成小铭牌质感。
5. **选路卡五类路专属图标**——云阶十行的安稳道/险道/宝库/星尘泉/影军道,现复用通用图标(stat-shield/state-crit/currency-coin/state-heal/state-shadow-echo)。做五枚专属 24px 语义图标,**按图标管线规范**(currentColor/双层珐琅),交付后由我进 UI_ICON_PATHS(动图标数量自检是我的活,你只交 SVG 到版本目录)。
6. **命脉条独立视觉**——HUD 命脉现复用暴走槽 DOM(`.life-pips`+`#rage-bar.life`)。若要独立 DOM,你可以动 `hud()` 里**命脉渲染那一段**(`_solo` 分支内),但绝不能碰 `S.life` 的读写逻辑与 `last-life` 类的开关条件。

**自装红线**(违反=回滚):
- 只动 CSS 与上面圈定的表现层 DOM;**绝不动** `simulateBattle`/数值/事件流/存档(`S`/`_meta`/`dyyw1`)/`BONDS`/`ZODIAC`/45 枚 `UI_ICON_PATHS` 管线/网络层(`LB_URL`/gpush/hpush/psave)。
- 新资产一律版本化目录(`*-2026071Xx`),不覆盖旧资产。
- 改后本地跑 `npm run selftest` 或推后盯 CI:**绿才算完成**;不改自检断言与总数(那是我的账)。
- 视觉验收基准:iPad 横屏 1194×834@2x / iPad 竖屏 834×1194@2x / 手机竖屏 430×932@3x;桌面只保兼容。
- reduced-motion 尊重;缺图回退不许破(探针/onerror 模式照旧)。

---

## B. 断云集市(账号级外观商店)——你出全部商品资产,我做经济与系统

**定位**:王老师拍板的新系统。局内经济(征募/刷新/淬炼)是核心玩法**不动**;集市是**局外**的账号级商店:学生用「**狼牙**」(复用狼牙悬赏品牌,完成悬赏/通关/成就获得)购买**纯外观**商品,多人零增益(名册铁律先例)。

**你要产的商品资产**(每类给 3–5 款,含 1 款"传世"稀有档;全部走版本化目录 `assets/bazaar-20260717/` + manifest):

| 商品类 | 规格 | 现挂点 |
|---|---|---|
| ① 征募卡背 | 与现 `.cardback` 显示位同比例,2x | 翻卡时刻,曝光最高 |
| ② 演武台皮肤 | 1600/800 双档透明 WebP,同 `arena-dais` 契约 | 战斗全程可见 |
| ③ 名片边框 | 适配 `.pcard` 尺寸的透明框,参照征募卡框工艺 | 榜上被人点开看,社交排面 |
| ④ 称号章框 | 环绕 5 档称号纹章的装饰框,currentColor SVG | 名册+名片 |
| ⑤ 星盘中心纹 | `#wheel-stage` 中心圆纹,currentColor SVG | 课堂公用,老师的排面 |

**manifest 要求**:每件商品 `{id, 类别, 中文名, 稀有档(白/蓝/紫/金/橙——吃全局色阶 TIER_COLOR), 建议狼牙价, 文件}`。中文名请起得有断云世界观味道(例:卡背「雾晨行军图」、台面「黑域王座」)。

**我这边负责**(你不用管):`_meta.fangs` 狼牙经济(幂等发放,防重复)、集市 UI(入口挂名册旁)、购买/装备系统(`_meta.owned`/`_meta.equip`,loadMeta 防御回填零迁移)、多人公平钉子、全套回归钉子。

**优先级**:A1–A4 你可先自装见效;B 资产量大,可分批交(先卡背+台面,曝光最高)。A5/A6 与 B 并行不冲突。

---

## 交接纪律(重申)

- 每完成一项,`AI-COLLABORATION.md` 追加一条:基线 SHA/改动文件/自测结果/给 Claude 的注意事项。
- 你自装的提交,commit message 前缀 `art(self):`——我好一眼识别哪些是你直接上线的,重点回归。
- 有拿不准的(比如某个 DOM 是否算表现层),宁可留言问我,不要猜。
