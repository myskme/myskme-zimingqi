# 自鸣棋 · 美工全权 + 新美术委托单（Claude → Codex）20260717 **v3**

> **本单取代 v2《修缮全权-20260717》（已删）。** 王老师重定分工，且把你的战场从「只做精已有」**扩到「新功能的全部新美术 + 新特效也归你」**，并要你**更主动、更大权限、自己发现自己完成**。

## 〇、一句话分工

- **Claude**：算得出 / 存得下 / 传得出 / 测得到的——`simulateBattle`、数值平衡、机制、事件流、存档、网络、自检、正典**数据**。Claude 会继续往下做新机制/新内容（逻辑侧）。
- **Codex（你）**：**看得见的一切**——CSS、表现层 DOM、资产、立绘、图标、动效、布局、色彩、特效、setpiece。**既把已有的做精，也为一切"新东西"配齐新美术+新特效。** 不用等派活，自己巡、自己立项、自己做、自己接线、自己上线。

## 一、你的主战场 = 新美术 + 新特效（本单新增重点）

1. **发挥 Image2**：新单位立绘（按《自鸣棋总账》+ 美术交接单锚点表）、新遗物/事件/牌面图标、新机制的 setpiece/开箱大幕、四象/星座/流派专属主视觉。
2. **新机制必配新特效（VFX）**：任何新机制——机制型流派质变（穿透必暴/亡语向死而生）、灵光法术牌、命脉、狼牙悬赏、影军回响、群英擂台……——都该有配套打击感/演出。母版你出、接线你做（自装，全流程你的）。
3. **主动认领**：巡 Claude 的新 commit、《自鸣棋总账》、以及游戏里任何「还是程序化占位 / 纯字 / 纯 CSS / 复用通用图标」的地方 → 立项升级成 Image2 精修美术。**看到空白就补，不必先问。**

## 二、全权范围 与 红线

- **全权（看得见的都归你，含图标 SVG 管线）**：新资产走版本化目录（如 `assets/xxx-2026MMDD/` + `manifest.json`），自己接线。
- **🚫红线（碰 = 回滚）**：`simulateBattle` / 数值平衡 / 事件流逻辑 / 存档（`S`·`_meta`·`dyyw1`·版本号·`snapDraft` 回滚律）/ 网络层（`LB_URL` 系列·pass 门）/ 正典角色的**数值·技能·`ZODIAC`·`BONDS` 的 id** / 自检的**逻辑断言**。
  —— 这些的**视觉呈现归你，数据本身归 Claude**。（例：一个新单位长什么样=你；它打多少伤害=Claude。）

## 三、已知施工地图（程序化占位，按价值排序；不限于此）

1. **传世开箱大幕** `.affix-veil`/`.av-*`——全游戏最高潮，现纯 CSS 金光。
2. **玩家名片** `.pcard`/`.pc-*`——榜上被点开的社交排面，现素 panel。
3. **名匠榜** `.lbrow`/`.hall-art` + 前三金银铜徽；**绝品实录** `.feedrow` 走马灯。
4. **点名星盘** 名牌质感 + `#wheel-stage` 中心底纹（你自己提过）。
5. **云阶十行选路卡**五路专属图标（安稳/险道/宝库/星尘泉/影军道，现复用通用图标）。
6. **命脉条独立视觉**（动 `hud()` 内 `_solo` 命脉渲染段，不碰 `S.life` 逻辑与 `last-life` 开关）。
7. **灵光法术牌 12 张牌面**（现吃色阶的程序化卡框，可上专属牌面/图标）。

## 四、自主巡检方向（举例，不限）

- **一致性**：按钮四态/面板边饰/弹窗标题/toast 质感跨屏统一；稀有度色只许一套（`TIER_COLOR`）。
- **高 DPR**：三档基准逐屏走查，细线糊/边缘裁/非等比拉伸零容忍。
- **动效手感**：入退场缓动统一；`reduced-motion` 全覆盖；一场战斗不过载（全屏演出白名单纪律）。
- **视觉 bug**：占位字漏底、层级互压、safe-area 裁切、暗色对比度——发现即修，日志留痕即可。

## 五、美术验收铁律（前几批踩过的坑，直接给你避）

- **Image2 立绘爱烤金环/金框**：顶部 38% 亮金像素必须扫干净；透明 alpha 四角、呼吸边 ≥19px；事件图左下留压字暗区。
- **`currentColor` 的 SVG 用 `<img>` 加载会变纯黑**（独立文档不继承色）→ 必须 `mask-image` + `background:currentColor`（还白捡全局色阶继承）。
- **mask 资源 404 会露纯色实心方块** → 所有 mask 装饰先用 `Image()` 探针探到再点亮（`body.cfok/rok/uok` 范式），缺图/离线/`file://` 一律回退原表现。
- **交付即接线（自装）**：读 `manifest.json` 不猜文件名；`@2x/@3x` srcset 锁 CSS px；短命演出截图用「冻结克隆」（去 transition + 克隆节点）。
- **色只用既有两套**：稀有度 `TIER_COLOR`（白`#9a9686`→蓝`#7fc4d8`→紫`#c77dff`→金`#ffd166`→橙`#ff8c42`）；四象 `ELEM_COLOR2`（风`#9cc0e8`/火`#e08b7a`/土`#c9a64a`/水`#7fc4d8`），数据源 `ZODIAC`。别自造第二套。
- **视觉基准**：iPad 横 1194×834@2x / iPad 竖 834×1194@2x / 手机竖 430×932@3x 高 DPR 逐屏验；**`≥1100px` 大字档保留**（围桌传一台 iPad 玩的硬需求，**不是投屏**，别砍）；桌面只保兼容。

## 六、工作纪律（自装协议）

1. 动手前 `git pull --rebase`；新资产版本化目录、不覆盖旧资产。
2. **每推确认 CI「自鸣棋自检」绿**（现基线 `#selftest 284/284`，HEAD `125f0ea`）。自检的**逻辑断言**是 Claude 的账、你不改；若是**图标/资产计数类**断言因你新增而变，更新计数即可；打红了先怀疑自己越了表现层，拿不准问 Claude。
3. 提交前缀 **`art(self):`**；每批在 `AI-COLLABORATION.md` 追加：基线 SHA / 改了什么 / 自测结果 / 给 Claude 的注意事项。
4. 🚫**断云禁令（王老师彻底否定，覆盖一切旧规则）**：**任何新命名——资产文件名、CSS 类、标签、称号、文案——绝不出现「断云」二字**。已有正典残留 NPC（断云斥候/星盘/箭袋/秘策/断云之上）不强改。
5. **定名勿改**：不屈命脉 / 绝品实录 / 云阶十行 / 鸣鼓开演 / 点名星盘 / 自鸣 / 群英擂台。**正典锚点必须吻合**（见总账+交接单；例：海=射手座=**火**象，名字「海」但忠实 `ZODIAC`）。文案只动你新视觉自带的标签，正文不动。
6. 拿不准某处算不算「表现层」→ 在日志问 Claude，别猜。

## 七、两 AI 同步

- 你的施工地图 = 仓库根目录《自鸣棋总账-人物剧情系统资产全录.md》（人物/剧情/系统/资产全录）+ 美术交接单锚点表。
- 你每批 `art(self):`，Claude 做回归走查（自检/存档/多人公平），有问题 Claude 修并在日志说明，**不动你的视觉意图**。
